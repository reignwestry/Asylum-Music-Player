import { View, Text, StyleSheet } from "react-native";

export default function Library() {
  return <View style={styles.ringtoneContainer}></View>;
}

const styles = StyleSheet.create({
  ringtoneContainer: {
    height: "100%",
    width: "100%",
  },
});
